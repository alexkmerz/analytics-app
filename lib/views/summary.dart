import 'package:flutter/material.dart';

class Summary extends StatelessWidget {
  Summary();

  @override
  Widget build(BuildContext context) {
    // Retrieve all recent analytics files

    return Scaffold(
      body: Text('Summary'),
    );
  }
}
